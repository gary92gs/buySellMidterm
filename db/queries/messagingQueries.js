// MAY REMOVE FROM PROJECT (DOING MAILTO AS PRIMARY MESSAGING METHOD)
const db = require('../connection');

const browseConversations = (userId) => {
  //find and list all conversations for a specific user
};

const getConversationThread = (conversationId) => {
  //find and list all messages for a specific conversation
};
