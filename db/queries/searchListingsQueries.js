const db = require('../connection');

const browseFeaturedListings = (searchFiltersObj) => {
  //grabs featured listings for main page
    //
    // do we want to have a featured section for each section??
};

const getListingCoverImage = (listingId) => {
  //grabs cover photo for each listing (probably the image with the lowest id)
}
